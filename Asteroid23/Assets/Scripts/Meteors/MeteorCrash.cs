using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class MeteorCrash : MonoBehaviour
{








































    /*[SerializeField] private List<GameObject> smallMeteor = new List<GameObject>();

    [SerializeField]
    private Transform spawnSmallMeteor;*/

    /*private void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.CompareTag(TagManager.PLAYER_TAG))
        {
            collision.GetComponent<PlayerHealth>().TakeDamage(10f);

            //IfDestroyed();
            Destroy(gameObject);

            //gameObject.SetActive(false);
        }
    }*/


    /*private void OnDestroy()
    {

        
    for (int i = 0; i < smallMeteor.Count; i++)
        {
            Instantiate(smallMeteor[i], spawnSmallMeteor.position, Quaternion.identity);
        }

    }*/

    /*void IfDestroyed()
    {
        for (int i = 0; i < smallMeteor.Count; i++)
        {
            Instantiate(smallMeteor[i],transform.position,Quaternion.identity);
        }
    }*/


}//class















